"use client";

import { useMemo, useState } from "react";
import Link from "next/link";

type CartItem = { productId: string; name: string; price: number; quantity: number };

function key(slug: string) { return `settlesmart-cart-${slug}`; }

export function AddToCart({ slug, item }: { slug: string; item: Omit<CartItem, "quantity"> }) {
  const [added, setAdded] = useState(false);
  return <button className="rounded-md bg-slate-950 px-4 py-2 text-sm font-semibold text-white" onClick={() => { const current = JSON.parse(sessionStorage.getItem(key(slug)) || "[]") as CartItem[]; const found = current.find((x) => x.productId === item.productId); if (found) found.quantity += 1; else current.push({ ...item, quantity: 1 }); sessionStorage.setItem(key(slug), JSON.stringify(current)); setAdded(true); }}>{added ? "Added" : "Add to cart"}</button>;
}

export function CartView({ slug }: { slug: string }) {
  const [items, setItems] = useState<CartItem[]>(() => {
    if (typeof window === "undefined") return [];
    return JSON.parse(sessionStorage.getItem(key(slug)) || "[]");
  });
  const total = useMemo(() => items.reduce((sum, item) => sum + item.price * item.quantity, 0), [items]);
  function save(next: CartItem[]) { setItems(next); sessionStorage.setItem(key(slug), JSON.stringify(next)); }
  if (items.length === 0) return <div className="rounded-lg border border-dashed border-slate-300 bg-white p-8 text-center"><h2 className="font-semibold">Your cart is empty</h2><Link href={`/order/${slug}`} className="mt-4 inline-flex rounded-md bg-slate-950 px-4 py-2 text-sm font-semibold text-white">Browse menu</Link></div>;
  return <div className="space-y-4">{items.map((item) => <div key={item.productId} className="flex flex-wrap items-center justify-between gap-3 border-b border-slate-100 py-3"><div><b>{item.name}</b><div className="text-sm text-slate-500">AED {item.price.toFixed(2)} · AED {(item.price * item.quantity).toFixed(2)}</div></div><div className="flex items-center gap-2"><button aria-label={`Decrease ${item.name}`} className="grid size-9 place-items-center rounded-md border border-slate-200 font-semibold" onClick={() => save(item.quantity === 1 ? items.filter((x) => x.productId !== item.productId) : items.map((x) => x.productId === item.productId ? { ...x, quantity: x.quantity - 1 } : x))}>-</button><span className="grid h-9 min-w-10 place-items-center rounded-md bg-slate-100 px-3 text-sm font-semibold">{item.quantity}</span><button aria-label={`Increase ${item.name}`} className="grid size-9 place-items-center rounded-md border border-slate-200 font-semibold" onClick={() => save(items.map((x) => x.productId === item.productId ? { ...x, quantity: x.quantity + 1 } : x))}>+</button><button className="rounded-md px-3 py-2 text-sm font-semibold text-red-600" onClick={() => save(items.filter((x) => x.productId !== item.productId))}>Remove</button></div></div>)}<div className="text-right text-lg font-semibold">Total AED {total.toFixed(2)}</div><Link href={`/order/${slug}/checkout`} className="inline-flex rounded-md bg-slate-950 px-4 py-2 text-sm font-semibold text-white">Checkout</Link></div>;
}

export function CheckoutForm({ slug }: { slug: string }) {
  const [error, setError] = useState("");
  return <form className="grid gap-3" onSubmit={async (event) => { event.preventDefault(); setError(""); const form = Object.fromEntries(new FormData(event.currentTarget)); const items = JSON.parse(sessionStorage.getItem(key(slug)) || "[]") as CartItem[]; const idempotencyKey = sessionStorage.getItem(`${key(slug)}-checkout`) || crypto.randomUUID(); sessionStorage.setItem(`${key(slug)}-checkout`, idempotencyKey); const response = await fetch("/api/public/orders", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ orderingSlug: slug, customer: { name: form.name, mobile: form.mobile, email: form.email, marketingConsent: Boolean(form.marketingConsent) }, address: form, fulfilmentType: form.fulfilmentType, paymentMethod: form.paymentMethod, promotionCode: form.promotionCode, idempotencyKey, specialInstructions: form.specialInstructions, items: items.map((i) => ({ productId: i.productId, quantity: i.quantity })) }) }); const json = await response.json(); if (!response.ok) { setError(json.error ?? "Checkout failed"); return; } sessionStorage.removeItem(key(slug)); sessionStorage.removeItem(`${key(slug)}-checkout`); location.href = `/order/${slug}/confirmation/${json.orderNumber}?token=${json.trackingToken}`; }}><input name="name" required placeholder="Name" className="rounded-md border border-slate-200 px-3 py-2" /><input name="mobile" required placeholder="Mobile" className="rounded-md border border-slate-200 px-3 py-2" /><input name="email" placeholder="Email optional" className="rounded-md border border-slate-200 px-3 py-2" /><input name="address" placeholder="Address" className="rounded-md border border-slate-200 px-3 py-2" /><input name="building" placeholder="Building" className="rounded-md border border-slate-200 px-3 py-2" /><input name="apartment" placeholder="Apartment" className="rounded-md border border-slate-200 px-3 py-2" /><input name="area" placeholder="Area" className="rounded-md border border-slate-200 px-3 py-2" /><input name="city" placeholder="City" className="rounded-md border border-slate-200 px-3 py-2" /><div className="grid grid-cols-2 gap-3"><input name="latitude" placeholder="Latitude optional" className="rounded-md border border-slate-200 px-3 py-2" /><input name="longitude" placeholder="Longitude optional" className="rounded-md border border-slate-200 px-3 py-2" /></div><button type="button" className="rounded-md border border-slate-200 px-3 py-2 text-sm" onClick={() => navigator.geolocation?.getCurrentPosition((pos) => { const form = document.querySelector("form") as HTMLFormElement; (form.elements.namedItem("latitude") as HTMLInputElement).value = String(pos.coords.latitude); (form.elements.namedItem("longitude") as HTMLInputElement).value = String(pos.coords.longitude); })}>Use current location</button><input name="promotionCode" placeholder="Promotion code optional" className="rounded-md border border-slate-200 px-3 py-2" /><textarea name="specialInstructions" placeholder="Order instructions" className="min-h-24 rounded-md border border-slate-200 px-3 py-2" /><select name="fulfilmentType" className="rounded-md border border-slate-200 px-3 py-2"><option>DELIVERY</option><option>PICKUP</option></select><select name="paymentMethod" className="rounded-md border border-slate-200 px-3 py-2"><option>CASH_ON_DELIVERY</option><option>CARD_ON_DELIVERY</option><option>CASH_ON_PICKUP</option><option>CARD_ON_PICKUP</option></select><label className="flex gap-2 text-sm"><input name="marketingConsent" type="checkbox" />Marketing consent</label><button className="rounded-md bg-slate-950 px-4 py-2 text-sm font-semibold text-white">Place order</button>{error && <p className="text-sm text-red-600">{error}</p>}</form>;
}
